# Mobbin Q5–Q6 fetch log

Task intent used on every call: `Research how top apps design scenarios for course management in an education platform`.

Queries run:

- Screens web/deep: `course content library duration skills metadata preview`
- Screens web/deep: `empty course outline add existing lesson template suggestions`
- Flows web: `start course from template content library`
- Screens ios/deep: `course content library duration skills metadata preview`
- Flows ios: `browse course library preview lesson details add`

Limitations: Mobbin returned adjacent analogues rather than exact LMS admin interfaces for several searches. Q5 evidence is strongest for visible metadata and catalog filtering; propagation semantics, competency-specific labels, and equipment metadata were not visible. Q6 evidence is mostly course builders and template libraries; no exact empty simulation-course screen surfaced. iOS results were learner-facing catalogs, so they are useful for metadata/preview but weak for instructor authoring. Assets were fetched after every search into `courses/05-scenarios/assets`; all kept images downloaded successfully.
